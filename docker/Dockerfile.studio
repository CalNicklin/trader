FROM node:22
WORKDIR /app

COPY package.json ./
RUN npm install

COPY src/db/schema.ts src/db/
COPY drizzle.config.ts ./

ENV DB_PATH=/app/data/trader.db
CMD ["npx", "drizzle-kit", "studio", "--host", "0.0.0.0"]
